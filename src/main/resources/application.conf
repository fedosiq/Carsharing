db = {
  connectionPool = "HikariCP"
  dataSourceClass = "org.postgresql.ds.PGSimpleDataSource"
  properties = {
    serverName = "localhost" //change to "db" for docker
    portNumber = "5432"
    databaseName = "carsharing"
    user = "postgres"
    password = "example"
  }
}
